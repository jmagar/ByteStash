{
  "name": "claudestash",
  "description": "A React and node.js app that stores Claude Code files with validation",
  "author": "jmagar (Originally forked from ByteStash by Jordan Dalby)",
  "version": "1.0.0",
  "main": "server/app.js",
  "scripts": {
    "start": "cd client && npm start",
    "server": "cd server && npm start",
    "dev": "docker-compose up --build"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "engines": {
    "node": ">=22"
  }
}
